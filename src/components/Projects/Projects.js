import React from 'react'

import Container from 'components/Container'

export default function Projects (props) {
  return (
    <Container fullscreen style={{backgroundColor: '#ddd'}} className={props.className}>
      projects
    </Container>
  )
}